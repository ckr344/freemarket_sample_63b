:css
  header{
    display: none;
  }
  footer{
    display: none;
  }
.user-new
  .user-new__logo
    = image_tag "logo/logo.png"
  .user-new__header
    %h2 会員情報入力
  .user-new__main
    = form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: "user-new__form"}) do |f|
      = render "devise/shared/error_messages", resource: resource
      .user-new__field
        = f.label :nickname, "ニックネーム", class: "user-new__field--title"
        = f.label :req, "必須", class: "user-new__field--req"
        %br/
        = f.text_field :nickname, autofocus: true, autocomplete: "nickname", placeholder: "例）メルカリ太郎", class: "user-new__field--input"
      .user-new__field
        = f.label :email, "メールアドレス", class: "user-new__field--title"
        = f.label :req, "必須", class: "user-new__field--req"
        %br/
        = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "PC・携帯どちらでも可", class: "user-new__field--input"
      .user-new__field
        = f.label :password, "パスワード", class: "user-new__field--title"
        = f.label :req, "必須", class: "user-new__field--req"
        %br/
        = f.password_field :password, autocomplete: "new-password", placeholder: "７文字以上の半角英数字", class: "user-new__field--input"
        %h2 ※英字と数字の両方を含めて設定してください
      .user-new__field
        .user-new__field--confirm
          %h3 本人確認
          %p1 安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
      .user-new__field
        = f.label :first_name, "お名前（全角）", class: "user-new__field--title"
        = f.label :req, "必須", class: "user-new__field--req"
        %br/
        = f.text_field :first_name, autofocus: true, placeholder: "例）山田", class: "user-new__field--name"
        = f.text_field :last_name, autofocus: true, placeholder: "彩", class: "user-new__field--name"
      .user-new__field
        = f.label :first_name, "お名前（全角カナ）", class: "user-new__field--title"
        = f.label :req, "必須", class: "user-new__field--req"
        %br/
        = f.text_field :first_name_kana, autofocus: true, placeholder: "例）ヤマダ", class: "user-new__field--name"
        = f.text_field :last_name_kana, autofocus: true, placeholder: "アヤ", class: "user-new__field--name"
      .user-new__field
        = f.label "生年月日", class: "user-new__field--title"
        = f.label :req, "必須", class: "user-new__field--req"
        %br/
        = f.select :birthday_yyyy, ["----"], {}, {class: "user-new__field--birth", id: "select_birth_year"}
        = f.label :birthday_yyyy, "年"
        = f.select :birthday_mm, ["--"], {}, {class: "user-new__field--birth", id: "select_birth_month"}
        = f.label :birthday_mm, "月"
        = f.select :birthday_dd, ["--"], {}, {class: "user-new__field--birth", id: "select_birth_day"}
        = f.label :birthday_dd, "日"
      .user-new__field
        .user-new__field--note
          %p1 ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
      .actions
        = f.submit "登録", class: "user-new__field--send"
      .user-new__field
        .user-new__field--end
          %p1 「登録」のボタンを押すことにより、利用規約に同意したものとみなします
  .user-new__licence
    = image_tag "logo/logo-white.png"
    %small © FRIMA, Inc.
  -# = render "devise/shared/links"